NODE generation_node
DESCRIPTION >
    Generation timeseries

SQL >
    SELECT
        toTimezone(datetime, 'Europe/Madrid') datetime,
        metric_name,
        value
    FROM  peninsular_demand_mv

TYPE ENDPOINT