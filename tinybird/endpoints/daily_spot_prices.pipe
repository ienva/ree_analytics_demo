NODE spot_node
DESCRIPTION >
    Spot Prices timeseries

SQL >
    SELECT
        toTimezone(datetime, 'Europe/Madrid') datetime,
        geo_name,
        value
    FROM daily_spot_mv FINAL

TYPE ENDPOINT